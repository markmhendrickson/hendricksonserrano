name: Deploy markh.io redirect

on:
  push:
    branches:
      - main
    paths:
      - 'markh-redirect/**'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages-markh"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages-markh
      url: https://markh.io
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Setup Pages
        uses: actions/configure-pages@v4
      
      - name: Copy redirect files
        run: |
          mkdir -p _site
          cp markh-redirect/index.html _site/
          cp markh-redirect/CNAME _site/
      
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '_site'
      
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

